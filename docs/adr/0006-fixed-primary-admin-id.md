# 唯一管理员使用固定身份主键

管理员聚合使用固定字符串 `_id = "primary-admin"`，管理员集合通过数据库级 JSON Schema 校验限制该取值，并由 MongoDB 的 `_id` 唯一约束保证系统最多存在一个管理员文档。JWT 的 `sub` 和管理会话的管理员引用始终使用该稳定主键，登录名修改不改变身份；初始化只允许插入，恢复只允许更新。该方案放弃随机 `ObjectId` 的通用性，以消除额外单例字段和仅靠应用约定维持唯一性的风险。
