# 管理会话采用状态型 JWT

管理会话使用 JWT 表达已签名的会话凭证，同时在 MongoDB 中保留以 `jti` 标识的管理会话记录。每次管理请求既验证 JWT，也校验对应会话的最近活动、绝对过期和撤销状态；主动退出、管理凭据修改和访问恢复因此可以立即撤销指定或全部会话。项目不采用纯无状态 JWT，因为其撤销延迟和空闲失效语义无法满足已经冻结的管理员安全边界；该方案接受每次请求读取会话状态的成本，以换取 JWT 形式与服务端可撤销能力同时成立。
