# 以环境变量配置单一管理员

第一期只有一个管理后台角色，因此不建立管理员账号表或多角色授权模型。部署环境提供管理员账号与密码哈希，登录成功后由后端签发后台会话令牌；读者的小程序身份与该管理员会话完全隔离，未来需要多人协作时再替换为账号体系。

## 考虑过的方案

- 多管理员账号表与角色权限：可扩展，但超出第一期单管理员范围。
- 不做后台认证：无法保护发布、下架、删除评论和 COS 上传签名等高权限操作。
