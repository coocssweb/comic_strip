# 使用会话世代原子撤销全部管理会话

唯一管理员聚合保存单调递增的 `sessionGeneration`，JWT 与管理会话记录保存签发时的世代值，每次管理鉴权同时校验当前管理员世代。修改管理凭据或执行访问恢复时，在单次管理员文档原子更新中变更权威状态并递增世代，使全部旧会话从后续鉴权开始立即失效；旧会话记录只需随后通过可重试清理移除。该方案增加一次管理员状态校验，但避免把正确性建立在管理会话逐条更新或 MongoDB 多文档事务上。
