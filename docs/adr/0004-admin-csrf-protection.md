# 管理写请求采用来源校验与会话绑定令牌双重防护

管理 API 对所有写请求同时校验精确 `Origin` 和会话绑定的 CSRF 令牌。登录成功及会话恢复响应提供与当前管理会话 `jti` 绑定的令牌，管理端只在内存中保存并通过自定义请求头发送，API 对照 MongoDB 会话记录校验；登录接口因尚无管理会话，只执行精确来源校验、登录限速和统一失败提示。管理 JWT 使用 API 主机专属的 `__Host-` Cookie，`SameSite=Strict` 仅作为附加防线，不替代上述校验，因为同站兄弟子域仍处于共同的站点边界内。
