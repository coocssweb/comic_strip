# 管理员登录限速使用 MongoDB 双维度短期状态

管理员登录同时检查来源 IP 和规范化登录名两个独立限速桶，状态保存在 MongoDB 的短期安全限速集合中，并通过 TTL 自动清理。桶键使用独立秘密进行 HMAC，不保存原始 IP 或登录名；限速在密码散列前执行，且不因 Node.js 进程重启而清空。该集合属于可丢弃的安全基础设施状态，不是领域实体、审计历史或管理员聚合；方案接受登录时的额外 MongoDB 原子更新，以避免引入 Redis 或依赖仅在单次进程生命周期内有效的内存状态。
