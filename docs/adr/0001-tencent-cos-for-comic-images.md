# 使用腾讯云 COS 存储漫画图片

四格漫画图片由管理后台上传，读者端需要稳定访问其 HTTPS 地址。选择腾讯云 COS 作为正式存储，避免将用户可访问的内容依赖于后端本地磁盘；本地磁盘仅可用于开发或演示，不作为生产存储方案。管理后台通过后端获取一次性预签名上传地址后直传 COS，后端不代理图片二进制，仅保存图片 HTTPS 地址。漫画图片公开读取，写入仅允许使用后端签发的短时预签名地址。

## 考虑过的方案

- 管理后台填写已有 HTTPS 图片地址：实现更快，但运营流程依赖外部上传工具。
- 后端本地磁盘存储：适合本地开发，不适合正式部署。
