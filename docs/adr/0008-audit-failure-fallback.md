# 安全审计失败不回滚已完成的权威动作

管理员状态和管理会话状态是权威事实，相关原子写入完成后再向独立集合追加安全审计。由于 MVP 不使用 MongoDB 多文档事务，审计写入失败时不回滚已经完成的密码修改、访问恢复、退出或会话撤销，也不把已完成动作对外报告为失败；系统改写 `error` 级结构化运行日志作为降级证据。该策略接受审计集合可能缺少个别记录的风险，以避免审计故障破坏安全状态或制造客户端重试歧义，并且不引入 outbox、消息队列或后台补写。
